---
page_title: "mistershell_worker Data Source - terraform-provider-mistershell"
subcategory: ""
description: |-
  Reads a MisterShell worker. Look up by id for a direct fetch, or by name (exact match). The authentication token is not returned by reads.
---

# mistershell_worker (Data Source)

Reads a MisterShell worker. Look up by id for a direct fetch, or by name (exact match). The authentication token is not returned by reads.

## Example Usage

```terraform
# Look up a worker by its integer ID.
data "mistershell_worker" "by_id" {
  id = 123
}

# Look up a worker by its exact name.
data "mistershell_worker" "by_name" {
  name = "emea-worker-1"
}

output "worker_status" {
  value = data.mistershell_worker.by_name.status
}

output "worker_location_id" {
  value = data.mistershell_worker.by_name.location_id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `id` (Number) Worker ID. Use for direct lookup.
- `name` (String) Worker name. Used as an exact-match search filter when id is not specified.

### Read-Only

- `active_task_count` (Number) Number of currently active tasks assigned to the worker.
- `config` (String) Opaque worker task-handler config as JSON.
- `config_schema` (String) JSON Schema used to validate the worker config, as JSON.
- `config_version` (String) Version identifier of the currently applied worker config.
- `connected_at` (String) Timestamp at which the worker connected.
- `created_at` (String) Creation timestamp.
- `description` (String) Worker description.
- `is_default` (Boolean) Whether this is the bootstrap default worker.
- `is_enabled` (Boolean) Whether the worker is enabled.
- `last_heartbeat_at` (String) Timestamp of the worker's last heartbeat.
- `location_id` (Number) ID of the location this worker belongs to.
- `presence_ip` (String) IP address from which the worker is connected.
- `presence_ip_source` (String) Source of the presence IP determination.
- `status` (String) Worker status.
- `total_task_count` (Number) Total number of tasks ever assigned to the worker.
- `updated_at` (String) Last update timestamp.

## Lookup arguments

Specify exactly one of:

| argument | behavior |
|---|---|
| `id` | Direct lookup by integer ID. |
| `name` | Exact-match lookup by name (fuzzy server search, then exact `name ==` filter). Errors if zero or more than one worker matches. |

## Returned values

The data source exposes every read field of a worker. The authentication
`token` is **never** returned by reads — it is shown only once at creation (see
the [`mistershell_worker` resource](../resources/worker.md) page).

| attribute | description |
|---|---|
| `id` | Worker integer ID. |
| `name` | Worker name. |
| `description` | Worker description. |
| `location_id` | Integer ID of the location the worker is bound to. |
| `status` | Operational state: `pending`, `online`, `offline`, or `error`. See the resource page. |
| `is_default` | `true` for the system-managed bootstrap default worker. |
| `is_enabled` | Whether the worker is enabled (admin intent). |
| `active_task_count` | Number of currently active tasks. |
| `total_task_count` | Total tasks ever assigned. |
| `last_heartbeat_at` | Timestamp of the last heartbeat. |
| `connected_at` | Timestamp the worker last connected. |
| `presence_ip` | IP the worker is connected from (or the manual override). |
| `presence_ip_source` | `detected` or `manual`. |
| `config` | Opaque task-handler config JSON (free-form). See the resource page. |
| `config_version` | SHA-256-derived version hash of the applied `config`. |
| `config_schema` | JSON Schema used to validate `config`, as JSON. |
| `created_at` | Creation timestamp. |
| `updated_at` | Last-update timestamp. |

For the `status` value meanings, the default-worker constraints, and the full
`config` / `config_schema` semantics, see the
[`mistershell_worker` resource](../resources/worker.md) page.
