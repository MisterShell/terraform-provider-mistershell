---
page_title: "mistershell_setting Data Source - terraform-provider-mistershell"
subcategory: ""
description: |-
  Reads a MisterShell application setting by key. value is JSON (heterogeneous bool/int/string). Secret-key values and defaults are masked as '****' by the API. Unknown keys return an error.
---

# mistershell_setting (Data Source)

Reads a MisterShell application setting by key. value is JSON (heterogeneous bool/int/string). Secret-key values and defaults are masked as '****' by the API. Unknown keys return an error.

## Example Usage

```terraform
# Read a single application setting by key.
data "mistershell_setting" "retention" {
  key = "security_log_retention_days"
}

# The value is JSON; decode it to use the native type.
output "retention_days" {
  value = jsondecode(data.mistershell_setting.retention.value)
}

output "retention_default" {
  value = jsondecode(data.mistershell_setting.retention.default)
}

output "retention_is_secret" {
  value = data.mistershell_setting.retention.is_secret
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `key` (String) Setting key to read. Must be a key in the backend settings registry.

### Read-Only

- `created_at` (String) Creation timestamp.
- `default` (String) Registry default value as JSON. Masked for non-empty secret keys.
- `description` (String) Human description of the setting.
- `is_secret` (Boolean) Whether the value is a secret.
- `updated_at` (String) Last update timestamp.
- `value` (String, Sensitive) Current value as JSON. Masked as '****' for secret keys.

## Lookup argument

| argument | behavior |
|---|---|
| `key` | Required. The registry setting key to read. Must exist in the backend registry — an unknown key returns an error. |

## Returned values

| attribute | description |
|---|---|
| `key` | The setting key. |
| `value` | Current value as JSON (sensitive). Decode with `jsondecode()`. **Masked as `****` for secret keys.** |
| `description` | Human-readable description from the registry. |
| `is_secret` | Whether the value is a secret (masked in responses). |
| `default` | Registry default value as JSON. Masked as `****` for non-empty secret keys. |
| `created_at` | Creation timestamp. |
| `updated_at` | Last update timestamp. |

For the complete list of valid `key` values — with each key's value type, range
or enum constraints, default, and whether it is secret — see the
[`mistershell_setting` resource](../resources/setting.md) page.
